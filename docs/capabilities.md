# MCP capabilities

The server exposes nine tools and no resources or prompts. Resources and prompts
are intentionally omitted because the current product has no stable read-only
URI model or reusable prompt templates.

Tool names, required arguments, defaults, input bounds and patterns, JSON
response mode, stateless Streamable HTTP behavior, and representative tool-call
success and validation failures are covered by contract tests.